filters:
  and:
    - list(categories).contains(link("Guides"))
    - '!file.name.contains("Template")'
views:
  - type: table
    name: All
    order:
      - file.name
      - related-project
      - status
      - areas
      - created
    sort:
      - property: created
        direction: DESC
