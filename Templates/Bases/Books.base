filters:
  and:
    - list(categories).contains(link("Books"))
    - '!file.name.contains("Template")'
views:
  - type: table
    name: All
    order:
      - file.name
      - author
      - via
      - status
      - areas
      - created
    sort:
      - property: created
        direction: DESC
