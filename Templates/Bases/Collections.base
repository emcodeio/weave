filters:
  and:
    - list(categories).contains(link("Collections"))
    - '!file.name.contains("Template")'
views:
  - type: table
    name: All
    order:
      - file.name
      - last
      - status
      - areas
      - created
    sort:
      - property: last
        direction: DESC
