filters:
  and:
    - list(categories).contains(link("Frameworks"))
    - '!file.name.contains("Template")'
views:
  - type: table
    name: All
    order:
      - file.name
      - topics
      - status
      - areas
      - created
    sort:
      - property: created
        direction: DESC
